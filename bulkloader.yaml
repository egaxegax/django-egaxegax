# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: _AE_Backup_Information_Kind_Files
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: files
      external_name: files
      # Type: String Stats: 1 properties of this type in this kind.


- kind: _AE_Backup_Information_Kind_Type_Info
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: entity_type_info
      external_name: entity_type_info
      # Type: Text Stats: 1 properties of this type in this kind.
      import_transform: db.Text


- kind: _AE_Backup_Information
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: complete_time
      external_name: complete_time
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: completed_jobs
      external_name: completed_jobs
      # Type: String Stats: 1 properties of this type in this kind.

    - property: filesystem
      external_name: filesystem
      # Type: String Stats: 1 properties of this type in this kind.

    - property: kinds
      external_name: kinds
      # Type: String Stats: 1 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 1 properties of this type in this kind.

    - property: start_time
      external_name: start_time
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: _AE_DatastoreAdmin_Operation
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: active_jobs
      external_name: active_jobs
      # Type: Integer Stats: 4 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: completed_jobs
      external_name: completed_jobs
      # Type: Integer Stats: 4 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: description
      external_name: description
      # Type: Text Stats: 4 properties of this type in this kind.
      import_transform: db.Text

    - property: last_updated
      external_name: last_updated
      # Type: Date/Time Stats: 4 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: status
      external_name: status
      # Type: String Stats: 4 properties of this type in this kind.

    - property: status_info
      external_name: status_info
      # Type: String Stats: 4 properties of this type in this kind.


- kind: auth_user
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: date_joined
      external_name: date_joined
      # Type: Date/Time Stats: 5 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: email
      external_name: email
      # Type: String Stats: 5 properties of this type in this kind.

    - property: first_name
      external_name: first_name
      # Type: String Stats: 5 properties of this type in this kind.

    - property: is_active
      external_name: is_active
      # Type: Boolean Stats: 5 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: is_staff
      external_name: is_staff
      # Type: Boolean Stats: 5 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: is_superuser
      external_name: is_superuser
      # Type: Boolean Stats: 5 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: last_login
      external_name: last_login
      # Type: Date/Time Stats: 5 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_name
      external_name: last_name
      # Type: String Stats: 5 properties of this type in this kind.

    - property: password
      external_name: password
      # Type: String Stats: 5 properties of this type in this kind.

    - property: username
      external_name: username
      # Type: String Stats: 5 properties of this type in this kind.


- kind: django_admin_log
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: action_flag
      external_name: action_flag
      # Type: Integer Stats: 15 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: action_time
      external_name: action_time
      # Type: Date/Time Stats: 15 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: change_message
      external_name: change_message
      # Type: Text Stats: 15 properties of this type in this kind.
      import_transform: db.Text

    - property: content_type_id
      external_name: content_type_id
      # Type: Integer Stats: 15 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: object_id
      external_name: object_id
      # Type: Text Stats: 15 properties of this type in this kind.
      import_transform: db.Text

    - property: object_repr
      external_name: object_repr
      # Type: String Stats: 15 properties of this type in this kind.

    - property: user_id
      external_name: user_id
      # Type: Integer Stats: 15 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: django_content_type
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: app_label
      external_name: app_label
      # Type: String Stats: 5 properties of this type in this kind.

    - property: model
      external_name: model
      # Type: String Stats: 5 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 5 properties of this type in this kind.


- kind: django_session
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: expire_date
      external_name: expire_date
      # Type: Date/Time Stats: 48 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: session_data
      external_name: session_data
      # Type: Text Stats: 48 properties of this type in this kind.
      import_transform: db.Text


- kind: guestbook_greeting
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: author_id
      external_name: author_id
      # Type: Integer Stats: 11 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: content
      external_name: content
      # Type: Text Stats: 11 properties of this type in this kind.
      import_transform: db.Text

    - property: date
      external_name: date
      # Type: Date/Time Stats: 11 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: subject_id
      external_name: subject_id
      # Type: Integer Stats: 11 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: guestbook_greeting_subject
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: subject
      external_name: subject
      # Type: String Stats: 7 properties of this type in this kind.


- kind: my_photo
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: album
      external_name: album
      # Type: String Stats: 14 properties of this type in this kind.

    - property: date
      external_name: date
      # Type: Date/Time Stats: 14 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: height
      external_name: height
      # Type: Integer Stats: 14 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: img
      external_name: img
      # Type: String Stats: 14 properties of this type in this kind.

    - property: thumb_url
      external_name: thumb_url
      # Type: String Stats: 14 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 14 properties of this type in this kind.

    - property: width
      external_name: width
      # Type: Integer Stats: 14 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: my_profile
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: about
      external_name: about
      # Type: Text Stats: 4 properties of this type in this kind.
      import_transform: db.Text

    - property: avatar
      external_name: avatar
      # Type: String Stats: 4 properties of this type in this kind.

    - property: user_id
      external_name: user_id
      # Type: Integer Stats: 4 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: my_song
  connector: csv
  connector_options:
    encoding: windows-1251
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.none_if_empty(int)

    - property: audio
      external_name: audio
      # Type: String Stats: 4 properties of this type in this kind.

    - property: author_id
      external_name: author_id
      # Type: Integer Stats: 6 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: content
      external_name: content
      # Type: Text Stats: 6 properties of this type in this kind.
      import_transform: db.Text

    - property: date
      external_name: date
      # Type: Date/Time Stats: 6 properties of this type in this kind.
      import_transform: transform.import_date_time('%d.%m.%Y %H:%M')
      export_transform: transform.export_date_time('%d.%m.%Y %H:%M')

    - property: title
      external_name: title
      # Type: String Stats: 6 properties of this type in this kind.

    - property: artist
      external_name: artist
